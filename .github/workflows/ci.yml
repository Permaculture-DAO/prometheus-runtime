name: runtime-s0-ci

on:
  pull_request:
  push:
    branches: [main, "release/**"]

permissions:
  contents: read

jobs:
  s0-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: S0 guard
        run: python scripts/s0_guard.py
      - name: Install validation dependencies
        run: pip install pyyaml==6.0.3
      - name: Runtime validation
        run: python scripts/validate_runtime.py
      - name: Contract validation
        run: |
          pip install jsonschema==4.26.0
          python scripts/validate_contracts.py
      - name: S4 ingestion guard
        run: python scripts/s4_guard.py
      - name: S5 normalization guard
        run: python scripts/s5_guard.py

  api-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install test dependencies
        working-directory: services/api
        run: pip install -r requirements.lock -r requirements-dev.lock
      - name: Run API tests
        working-directory: services/api
        run: PYTHONPATH=. pytest -q

  node-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: "20.19.4"
      - name: Syntax check bridge
        working-directory: services/holochain_bridge
        run: node --check src/server.js
